import 'package:flutter/material.dart';

class PokeMovesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('MOVES'),
    );
  }
}
